<h1>Recipe Index Page</h1>
<p>
<% @recipes.each do |recipe|%>
  <%= link_to(image_tag(recipe.picture.image_url(:thumb)), polymorphic_url([@recipeable, recipe])) %>
  <br/>
  <%= recipe.name %>
  <br/>
<% end %>
</p>
